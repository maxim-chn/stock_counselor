import { CollectStockDataResponseContainerDirective } from './collect-stock-data-response-container.directive';

describe('CollectStockDataResponseContainerDirective', () => {
  it('should create an instance', () => {
    const directive = new CollectStockDataResponseContainerDirective();
    expect(directive).toBeTruthy();
  });
});

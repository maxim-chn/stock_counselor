import { CollectStockDataRequestContainerDirective } from './collect-stock-data-request-container.directive';

describe('CollectStockDataRequestContainerDirective', () => {
  it('should create an instance', () => {
    const directive = new CollectStockDataRequestContainerDirective();
    expect(directive).toBeTruthy();
  });
});

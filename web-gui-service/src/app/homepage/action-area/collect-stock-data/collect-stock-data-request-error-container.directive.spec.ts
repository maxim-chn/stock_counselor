import { CollectStockDataRequestErrorContainerDirective } from './collect-stock-data-request-error-container.directive';

describe('CollectStockDataRequestErrorContainerDirective', () => {
  it('should create an instance', () => {
    const directive = new CollectStockDataRequestErrorContainerDirective();
    expect(directive).toBeTruthy();
  });
});

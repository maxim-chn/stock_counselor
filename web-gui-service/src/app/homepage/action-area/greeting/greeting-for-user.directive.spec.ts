import { GreetingForUserDirective } from './greeting-for-user.directive';

describe('GreetingForUserDirective', () => {
  it('should create an instance', () => {
    const directive = new GreetingForUserDirective();
    expect(directive).toBeTruthy();
  });
});

import { CalculateInvestmentRecommendationRequestContainerDirective } from './calculate-investment-recommendation-request-container.directive';

describe('CalculateInvestmentRecommendationRequestContainerDirective', () => {
  it('should create an instance', () => {
    const directive = new CalculateInvestmentRecommendationRequestContainerDirective();
    expect(directive).toBeTruthy();
  });
});

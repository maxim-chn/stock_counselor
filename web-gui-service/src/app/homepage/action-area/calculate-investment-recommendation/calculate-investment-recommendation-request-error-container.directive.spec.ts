import { CalculateInvestmentRecommendationRequestErrorContainerDirective } from './calculate-investment-recommendation-request-error-container.directive';

describe('CalculateInvestmentRecommendationRequestErrorContainerDirective', () => {
  it('should create an instance', () => {
    const directive = new CalculateInvestmentRecommendationRequestErrorContainerDirective();
    expect(directive).toBeTruthy();
  });
});

import { CalculateInvestmentRecommendationResponseContainerDirective } from './calculate-investment-recommendation-response-container.directive';

describe('CalculateInvestmentRecommendationResponseContainerDirective', () => {
  it('should create an instance', () => {
    const directive = new CalculateInvestmentRecommendationResponseContainerDirective();
    expect(directive).toBeTruthy();
  });
});
